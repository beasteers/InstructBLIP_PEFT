datasets:
  scienceqa:
    # data_dir: ${env.data_dir}/datasets
    data_type: images # [images|videos|features]

    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          storage: ../../input/s20bn/train.json
        val:
          storage: ../../input/s20bn/val.json
        test:
          storage: ../../input/s20bn/test.json
      images:
        storage: ../../input/s20bn
        train:
          storage: ../../input/s20bn
        val:
          storage: ../../input/s20bn
        test:
          storage: ../../input/s20bn
